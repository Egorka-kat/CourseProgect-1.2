<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
                     xmlns:fa="http://schemas.fontawesome.io/icons/">
    <!-- Основные цвета -->
    <Color x:Key="BackgroundColor">#FF121212</Color>
    <Color x:Key="ForegroundColor">#FFE0E0E0</Color>
    <Color x:Key="BorderColor">#FF424242</Color>
    <Color x:Key="AccentColor">#FFBB86FC</Color>
    <Color x:Key="MenuBackgroundColor">#FF1E1E1E</Color>
    <Color x:Key="HighlightColor">#FF3700B3</Color>
    <Color x:Key="SuccessColor">#FF03DAC6</Color>
    <Color x:Key="WarningColor">#FFFFB300</Color>
    <Color x:Key="ErrorColor">#FFCF6679</Color>

    <!-- Цвета вкладок -->
    <Color x:Key="TabItemBackground">#FF1E1E1E</Color>
    <Color x:Key="TabItemForeground">#FFE0E0E0</Color>
    <Color x:Key="TabItemBorder">#FF424242</Color>
    <Color x:Key="TabItemSelectedBackground">#FF2D2D2D</Color>
    <Color x:Key="TabItemSelectedForeground">#FFFFFFFF</Color>
    <Color x:Key="TabItemSelectedBorder">#FFBB86FC</Color>
    <Color x:Key="TabItemHoverBackground">#FF252525</Color>

    <!-- Цвета текстовых редакторов -->
    <Color x:Key="TextEditorBackground">#FF1E1E1E</Color>
    <Color x:Key="TextEditorForeground">#FFE0E0E0</Color>
    <Color x:Key="TreeViewBackground">#FF1A1A1A</Color>
    <Color x:Key="TreeViewForeground">#FFE0E0E0</Color>
    <Color x:Key="TreeViewItemForeground">#FFE0E0E0</Color>
    <Color x:Key="TabControlBackground">#FF1E1E1E</Color>
    <Color x:Key="TabControlForeground">#FFE0E0E0</Color>
    <Color x:Key="ListBorderColor">#FF424242</Color>

    <!-- Цвета кнопок -->
    <Color x:Key="ButtonBackground">#FF2D2D2D</Color>
    <Color x:Key="ButtonForeground">#FFE0E0E0</Color>
    <Color x:Key="ButtonHoverBackground">#FF3D3D3D</Color>

    <!-- Цвета списков -->
    <Color x:Key="ListBoxBackground">#FF1E1E1E</Color>
    <Color x:Key="ListBoxForeground">#FFE0E0E0</Color>

    <!-- Кисти -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="{StaticResource MenuBackgroundColor}"/>

    <!-- Кисти вкладок -->
    <SolidColorBrush x:Key="TabItemBackgroundBrush" Color="{StaticResource TabItemBackground}"/>
    <SolidColorBrush x:Key="TabItemForegroundBrush" Color="{StaticResource TabItemForeground}"/>
    <SolidColorBrush x:Key="TabItemBorderBrush" Color="{StaticResource TabItemBorder}"/>
    <SolidColorBrush x:Key="TabItemSelectedBackgroundBrush" Color="{StaticResource TabItemSelectedBackground}"/>
    <SolidColorBrush x:Key="TabItemSelectedForegroundBrush" Color="{StaticResource TabItemSelectedForeground}"/>
    <SolidColorBrush x:Key="TabItemSelectedBorderBrush" Color="{StaticResource TabItemSelectedBorder}"/>
    <SolidColorBrush x:Key="TabItemHoverBackgroundBrush" Color="{StaticResource TabItemHoverBackground}"/>

    <!-- Кисти редакторов -->
    <SolidColorBrush x:Key="TextEditorBackgroundBrush" Color="{StaticResource TextEditorBackground}"/>
    <SolidColorBrush x:Key="TextEditorForegroundBrush" Color="{StaticResource TextEditorForeground}"/>
    <SolidColorBrush x:Key="TreeViewBackgroundBrush" Color="{StaticResource TreeViewBackground}"/>
    <SolidColorBrush x:Key="TreeViewForegroundBrush" Color="{StaticResource TreeViewForeground}"/>
    <SolidColorBrush x:Key="TreeViewItemForegroundBrush" Color="{StaticResource TreeViewItemForeground}"/>
    <SolidColorBrush x:Key="TabControlBackgroundBrush" Color="{StaticResource TabControlBackground}"/>
    <SolidColorBrush x:Key="TabControlForegroundBrush" Color="{StaticResource TabControlForeground}"/>
    <SolidColorBrush x:Key="ListBorderBrush" Color="{StaticResource ListBorderColor}"/>

    <!-- Акцентные кисти -->
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="HighlightBrush" Color="{StaticResource HighlightColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

    <!-- Кисти контролов -->
    <SolidColorBrush x:Key="ListBoxBackgroundBrush" Color="{StaticResource ListBoxBackground}"/>
    <SolidColorBrush x:Key="ListBoxForegroundBrush" Color="{StaticResource ListBoxForeground}"/>
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="{StaticResource ButtonBackground}"/>
    <SolidColorBrush x:Key="ButtonForegroundBrush" Color="{StaticResource ButtonForeground}"/>
    <SolidColorBrush x:Key="ButtonHoverBackgroundBrush" Color="{StaticResource ButtonHoverBackground}"/>




    <!-- Стили -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource MenuBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>




    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource HighlightBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Стиль для ContextMenu -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <!-- TreeView стили -->
    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource TreeViewBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TreeViewForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{StaticResource TreeViewForegroundBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource HighlightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Стиль для TabControl -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource TabControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border x:Name="HeaderBorder"
                            Grid.Row="0"
                            Background="{StaticResource TabControlBackgroundBrush}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0,0,0,1">
                            <TabPanel x:Name="HeaderPanel"
                                  IsItemsHost="True"
                                  Margin="0,0,0,0"/>
                        </Border>

                        <Border x:Name="ContentBorder"
                            Grid.Row="1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                          ContentSource="SelectedContent"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <!-- TextEditor стили -->
    <Style TargetType="avalonEdit:TextEditor">
        <Setter Property="Background" Value="{StaticResource TextEditorBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextEditorForegroundBrush}"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="12pt"/>
        <Setter Property="ShowLineNumbers" Value="True"/>
        <Setter Property="WordWrap" Value="False"/>
    </Style>

    <!-- Упрощенный стиль для TabItem -->
    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{StaticResource TabItemBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TabItemForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TabItemBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1,1,1,0"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="0,0,-1,0"/>

        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource TabItemSelectedBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TabItemSelectedForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource TabItemSelectedBorderBrush}"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource TabItemHoverBackgroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ContentPresenter x:Name="ContentSite"
                                          ContentSource="Content"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource ButtonHoverBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="Black"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource TextEditorBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextEditorForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource HighlightBrush}"/>
        <Setter Property="SelectionTextBrush" Value="White"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Стиль Label -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource ListBoxBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ListBoxForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>    

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource ListBoxForegroundBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4,2"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource HighlightBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource TabItemHoverBackgroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
    </Style>

    <Style x:Key="VisibleMenuItem" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
        <Setter Property="Visibility" Value="Visible"/>
        <Setter Property="Background" Value="{StaticResource TreeViewBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TreeViewForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <Style x:Key="CollapsedMenuItem" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>

    <Style TargetType="fa:ImageAwesome">
        <Setter Property="Foreground" Value="White"/>
    </Style>
</ResourceDictionary>