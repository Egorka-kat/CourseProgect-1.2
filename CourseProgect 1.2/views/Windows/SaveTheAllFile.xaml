<Window x:Class="CourseProgect_1._2.views.Windows.SaveTheAllFile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CourseProgect_1._2.ViewModels"
        xmlns:local="clr-namespace:CourseProgect_1._2.views.Windows"
        mc:Ignorable="d"
        Title="TextEditor" Height="245" Width="300" WindowStyle="SingleBorderWindow" ResizeMode="NoResize">
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top">
                <TextBlock Margin="25,5,25,0">Сохранить ихменения в текущих файлах?</TextBlock>
            </StackPanel>
            <Grid DockPanel.Dock="Top">
                <ListBox  x:Name="LB_SelectItem" Margin="25,5,25,5" Height="130" 
         ItemsSource="{Binding TabItems}"
         SelectedItem="{Binding SelectedTabIndex}"
         SelectionMode="Multiple">

                    <!-- Шаблон для элементов списка -->
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TitleName}">
                                <TextBlock.ToolTip>
                                    <ToolTip Background="White" Foreground="Black" 
                             HasDropShadow="False" Placement="Relative" 
                             HorizontalOffset="15" VerticalOffset="10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Path}"/>
                                        </StackPanel>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <Grid DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="6*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Margin="25,8,5,12" Content="Сохранить" Click="SaveButton"/>
                <Button Grid.Column="1" Margin="15,8,15,12" Content="Не Сохранять" Click="OnSaveButton"/>
                <Button Grid.Column="2" Margin="5,8,25,12" Content="Отмена" Click="CalcerButton"/>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
